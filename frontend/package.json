{
  "name": "barbearia-maraca-frontend",
  "version": "1.0.0",
  "description": "Front-end da Barbearia Maracá — Vanilla TypeScript + HTML5 + CSS3 SPA",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "vitest": "^5.0.0"
  }
}
